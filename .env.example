# =============================================================================
# GitHub event intake mode
# =============================================================================
# How ProPR receives GitHub events. New installs use the hosted ProPR GitHub App
# with WebSocket routing — no public URL and no GitHub App of your own required.
# This is the default and the normal path; polling and direct webhook below are
# advanced opt-ins.
#   routing_websocket — events stream over the ProPR routing WebSocket (default)
#   polling           — ProPR pulls events from the GitHub API (advanced opt-in)
#   direct_webhook    — GitHub delivers events to your own public /webhook URL
#                       (advanced; requires your own GitHub App)
#
# Migrating an existing install: the default is now routing_websocket. If you run
# your OWN GitHub App (the old polling/webhook setup), routing_websocket will not
# work for you — it needs the hosted ProPR App via the relay (relay auth mode).
# Keep your current behavior by setting this explicitly to `polling` or
# `direct_webhook`, or migrate to the hosted path with `propr relay enroll`.
GITHUB_EVENT_INTAKE_MODE=routing_websocket

# --- Routing WebSocket (default intake) --------------------------------------
# The hosted path: ProPR authenticates through the shared ProPR GitHub App via
# the token relay and streams events over the routing WebSocket. Run
# `propr relay enroll` to populate PROPR_GH_RELAY_URL / PROPR_GH_RELAY_TOKEN /
# GH_INSTALLATION_ID for you, or set them by hand below.
#   PROPR_ROUTING_URL    — routing WebSocket origin (wss://; ws:// only for localhost)
#   PROPR_GH_RELAY_URL   — token relay URL incl. version prefix
#                          (https://; http only for localhost)
#   PROPR_GH_RELAY_TOKEN — durable relay credential issued for your install
#   GH_INSTALLATION_ID   — which GitHub App installation ProPR acts on
# Both URLs default to the hosted relay (webhook.propr.dev) when unset, so you
# only need them below to point at a self-hosted relay.
# PROPR_ROUTING_URL=wss://webhook.propr.dev
# PROPR_GH_RELAY_URL=https://webhook.propr.dev/v1
# Optional transport keepalive override. The default is 300000 ms (5 minutes);
# lower it only if a network path closes otherwise-healthy WebSockets too quickly.
# PROPR_ROUTING_WS_PING_INTERVAL_MS=300000
# PROPR_ROUTING_WS_PONG_TIMEOUT_MS=30000
# Per-install secrets — left commented so an unedited copy is not mistaken for a
# configured relay. Run `propr relay enroll` to fill these in, or uncomment and set
# them by hand. (Active placeholder values here could make intake/auth mode look
# configured when it is not.)
# PROPR_GH_RELAY_TOKEN=your_relay_token
# GH_INSTALLATION_ID=your_installation_id
# Optional: force the auth mode explicitly instead of inferring (app | relay | demo).
# Relay mode is inferred automatically when PROPR_GH_RELAY_URL + PROPR_GH_RELAY_TOKEN
# are set, so you normally do not need this.
# GH_AUTH_MODE=relay

# Advanced override for the GitHub user credential used only for visual-preview
# attachment uploads. An administrator's Web UI login can be captured when it
# uses a GitHub OAuth App token (`gho_`); GitHub's uploader rejects GitHub App
# user (`ghu_`) and installation (`ghs_`) tokens. Set this to override the Web
# UI flow with an OAuth App token, classic PAT, or fine-grained PAT for a user
# with write access to each target repository.
# GITHUB_VISUAL_PREVIEW_TOKEN=your_oauth_or_personal_access_token
# Optional dedicated encryption secret for the persisted OAuth grant. The
# default is SYSTEM_TASK_SECRET, falling back to SESSION_SECRET. Keep the value
# stable and identical for the API and worker.
# PROPR_CREDENTIAL_ENCRYPTION_KEY=generate-a-strong-secret-here

# --- Hosted UI tunnel (v1, optional) -----------------------------------------
# Expose this local stack's API to the hosted control plane at
# https://app.propr.dev through a Cloudflare Tunnel, so you can drive a
# locally-running stack from the hosted UI. The tunnel publishes the API
# container (only /api/* and /socket.io/* are routed; the root URL returns 404);
# the UI bundle is served by app.propr.dev, not through the tunnel. Cloudflare
# forwards the tunnel to the Docker-internal http://api:4000, NOT host port 4000,
# so the published host port is irrelevant to the tunnel and cannot conflict with
# it. Off by default; local development is unaffected (API_PUBLIC_URL /
# FRONTEND_URL keep their localhost defaults below).
#
# Normal setup is CLI-first: ProPR Connect shows a one-time command such as
# `propr tunnel setup --token ... --url https://t-<id>.propr.dev --start`.
# Run that from this stack directory so the CLI writes these values and restarts
# the stack for you. The variables below are documented as a fallback for older
# CLI versions, manual recovery, or auditing what the setup command persists.
#
# Enable the tunnel:
#   PROPR_UI_TUNNEL_TOKEN   — Cloudflare Tunnel token; required to start. Setting it
#                             enables the tunnel by default, so the next `propr start`
#                             brings up the sidecar (unless you ran `propr tunnel off`).
#                             WARNING: this is a LIVE Cloudflare credential — anyone
#                             with it can route traffic through your tunnel. Do not
#                             commit, log, or share it; keep it in this .env only.
#   PROPR_UI_TUNNEL_ENABLED — explicitly enable the tunnel (`true`/`1`). A token is
#                             STILL required — `propr check` fails if this is set
#                             without PROPR_UI_TUNNEL_TOKEN. Redundant when a token
#                             is set, since a token alone already enables the tunnel
#   PROPR_INSTANCE_ID       — this stack's instance id; must be a valid DNS label
#                             (letters, digits, hyphens; 1-61 chars). Derives the
#                             public URL https://t-<id>.propr.dev when no
#                             explicit URL is set
#   PROPR_UI_PUBLIC_API_URL — explicit public API URL the hosted UI talks to (overrides the derived one)
#   PROPR_CLOUDFLARED_IMAGE — cloudflared image (default: cloudflare/cloudflared:2024.12.2,
#                             a pinned tag; override only if you need a different build)
# PROPR_UI_TUNNEL_TOKEN=your_cloudflare_tunnel_token
# PROPR_INSTANCE_ID=abc123
# PROPR_UI_PUBLIC_API_URL=https://t-abc123.propr.dev
# PROPR_CLOUDFLARED_IMAGE=cloudflare/cloudflared:2024.12.2
#
# Why `t-*.propr.dev`: each enabled stack is published under a per-instance
# hostname `t-<PROPR_INSTANCE_ID>.propr.dev`. The hosted UI at
# https://app.propr.dev discovers and reaches your stack's API through that
# managed hostname pattern, without you owning or registering a domain.
#
# Browser origin vs API host — they are DIFFERENT and must be set accordingly.
# The browser loads the UI from app.propr.dev; the API, Socket.IO, OAuth
# callback, and session cookie all live on the proxy host. In tunnel mode
# FRONTEND_URL, API_PUBLIC_URL, and GH_OAUTH_CALLBACK_URL are derived
# automatically (FRONTEND_URL -> https://app.propr.dev, API_PUBLIC_URL -> the
# proxy host, GH_OAUTH_CALLBACK_URL -> <proxy host>/api/auth/github/callback);
# set them only to override. Hosted tunnel login is handed off to ProPR Connect
# and the shared ProPR GitHub App; no OAuth App or callback registration of your
# own is required.
# FRONTEND_URL=https://app.propr.dev
# API_PUBLIC_URL=https://t-abc123.propr.dev
# GH_OAUTH_CALLBACK_URL=https://t-abc123.propr.dev/api/auth/github/callback
#
# COOKIE_DOMAIN: leave UNSET for v1. The session cookie is host-only on the
# single `t-<id>.propr.dev` host — correct because that host and
# app.propr.dev share the propr.dev registrable domain (same-site). Setting
# COOKIE_DOMAIN (e.g. `.propr.dev`) would scope the cookie across every
# ProPR-managed host and is not supported for v1 proxy sessions.
# COOKIE_DOMAIN=

# Logging Configuration
LOG_LEVEL=info
NODE_ENV=development
# Privacy-safe API latency attribution is disabled by default. Temporarily set
# a bounded sample rate from 0 to 1 (for example 0.01) to log static route/stage
# names and aggregate durations. Request URLs, parameters, headers, bodies,
# credentials and SQL are never included.
# PROPR_API_TIMING_SAMPLE_RATE=0.01

# Daemon Configuration
# Optional static override. Leave unset to use repositories selected through
# guided setup or the Settings UI; those persisted selections reload live.
# GITHUB_REPOS_TO_MONITOR=owner/repo1,owner/repo2

# --- Polling intake (advanced opt-in) ----------------------------------------
# Polling is an explicit opt-in alternative to the default routing WebSocket.
# It is only used when GITHUB_EVENT_INTAKE_MODE=polling (set above); leave the
# intake mode at routing_websocket to keep the hosted path. In polling mode
# ProPR pulls events from the GitHub API on this interval using any usable
# GitHub auth (relay or your own App). POLLING_INTERVAL_MS is the poll period in
# milliseconds (default: 60000).
POLLING_INTERVAL_MS=60000

# Config Repository (for dynamic repository management)
# CONFIG_REPO=https://github.com/your-org/propr-config.git

# Issue Detection Configuration
PRIMARY_PROCESSING_LABELS=AI,propr
PR_LABEL=propr

# Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# The development docker-compose.yml publishes Redis on loopback by default.
# Override the bind host only when trusted Docker bridge clients must connect;
# never bind this unauthenticated port to a public interface.
# REDIS_EXTERNAL_BIND_HOST=127.0.0.1
# REDIS_EXTERNAL_PORT=6380

# Queue Configuration
GITHUB_ISSUE_QUEUE_NAME=github-issue-processor
WORKER_CONCURRENCY=2
TASK_STATE_RECONCILIATION_INTERVAL_MS=60000
TASK_STATE_RECONCILIATION_STALE_MS=900000
TASK_STATE_RECONCILIATION_BATCH_SIZE=100
TASK_STATE_RECONCILIATION_TIME_BUDGET_MS=30000
COMMENT_BATCH_DELAY_MS=3000

# Lightweight agent calls include container startup time. Five seconds is too
# short for a cold Codex launch; keep enough headroom for the title response.
# PR_TASK_TITLE_GENERATION_TIMEOUT_MS=30000

# Summarization fallback/cooldown controls
# Promote the configured summarization fallback to primary after this many
# primary quota failures for the same agent/model. Defaults to 3.
# SUMMARIZATION_FALLBACK_PROMOTE_THRESHOLD=3
# Pause normal summarization jobs for a repository/branch after both primary
# and fallback paths fail. Defaults to 3600000 (1 hour).
# SUMMARIZATION_QUOTA_COOLDOWN_MS=3600000

# --- Direct webhook intake (advanced) ----------------------------------------
# Direct webhook is an advanced alternative to the default routing WebSocket.
# It is only used when GITHUB_EVENT_INTAKE_MODE=direct_webhook (set near the top
# of this file). This path requires your OWN GitHub App (not the hosted ProPR
# App) and a publicly reachable POST /webhook URL that GitHub can deliver events
# to — the endpoint is served by the dashboard API service (port 4000). Because
# of those requirements, most installs should stay on routing_websocket.
#
# Own GitHub App credentials (app auth mode — required for direct webhook):
#   GH_APP_ID            — your GitHub App's numeric id
#   GH_PRIVATE_KEY_PATH  — path to your App's private key (.pem)
#   GH_INSTALLATION_ID   — which installation to act on (set in the routing
#                          section above; reused here)
# GH_APP_ID=your_app_id
# GH_PRIVATE_KEY_PATH=./path/to/your-app-private-key.pem
# Host path to the GitHub App private key (.pem). Recommended when running via
# the `propr` CLI or launcher: the key is bind-mounted (read-only) into the app
# containers and GH_PRIVATE_KEY_PATH above is overridden to point at it, so you
# can keep the key anywhere on the host instead of staging it under data/.
# Must be an absolute host path (no '~').
# HOST_GH_PRIVATE_KEY=/home/your-user/propr/app-private-key.pem
# Shared secret GitHub signs webhook deliveries with; required for this mode.
# GH_WEBHOOK_SECRET=your-webhook-secret

# Deprecated: ENABLE_GITHUB_WEBHOOKS no longer selects the intake mode. Use
# GITHUB_EVENT_INTAKE_MODE instead (routing_websocket | polling | direct_webhook).
# Documented here only so existing .env files are recognized; do not rely on it.
# ENABLE_GITHUB_WEBHOOKS=false

# System Task Authorization
# Secret used to sign system task requests (e.g., revert operations)
# Generate with: openssl rand -hex 32
SYSTEM_TASK_SECRET=
# Maximum age (ms) for signed system task tokens. Increase if jobs frequently expire
# due to queue backlog or worker downtime. Defaults to 7200000 (2 hours).
# SYSTEM_TASK_TOKEN_MAX_AGE_MS=7200000

# Worker Configuration

# PR Comment Monitoring Configuration
GITHUB_BOT_USERNAME=propr.dev[bot]
GITHUB_USER_WHITELIST=
GITHUB_USER_BLACKLIST=
# Optional comma-separated GitHub usernames that bootstrap instance
# administrators. Administrators can manage settings, agents, access roles, and
# the trusted agent runtime. Durable role assignments are managed in Web UI >
# Access. Set at least one username for initial setup; non-demo startup refuses
# to continue when neither this list nor a durable administrator exists.
# Unassigned users are members. GitHub usernames can be renamed or recycled, so
# use Access to store the bootstrap administrator by numeric GitHub ID, then
# remove or carefully maintain this list. Keep an entry only if you want a
# username-based break-glass administrator.
PROPR_ADMIN_USERS=
PR_FOLLOWUP_TRIGGER_KEYWORDS=!propr
# With a whitelist set, polling resolves who applied the trigger label from the
# issue timeline (page 1 + the most recent N pages). Raise this if long-lived
# issues with very long timelines are skipped with "Could not determine label
# applier". Default: 5.
# LABEL_APPLIER_TIMELINE_MAX_PAGES=5

# Git Configuration
# Optional host-only root for temporary directories bind-mounted into child
# agent containers. For example, /srv/propr-alt maps /tmp/git-processor to
# /srv/propr-alt/git-processor while the in-container path stays unchanged.
# Unset this to keep the existing host paths.
# Pair it with a distinct PROPR_STACK value for each concurrent instance.
# PROPR_HOST_TEMP_ROOT=/srv/propr-alt
GIT_CLONES_BASE_PATH=/tmp/git-processor/clones
GIT_WORKTREES_BASE_PATH=/tmp/git-processor/worktrees
GIT_DEFAULT_BRANCH=main
GIT_SHALLOW_CLONE_DEPTH=

# Agent Runtime Configuration
AGENT_DOCKER_IMAGE=propr/agent:latest
# Every coding-agent execution has a host-level Docker resource ceiling.
# Raise these for unusually large repositories; invalid or zero values fail
# closed before the agent container starts.
AGENT_CONTAINER_MEMORY_LIMIT=6g
# When unset, ProPR uses the smaller of 4 CPUs and the capacity detected by the
# worker. Set this only to override that adaptive default.
# AGENT_CONTAINER_CPU_LIMIT=4
AGENT_CONTAINER_PIDS_LIMIT=512
# Agents added with "Log in to a new account" use an isolated, ProPR-managed
# credential directory automatically. Native and Compose installs store these
# below ~/.propr/agent-credentials; the launcher container stores them below
# PROPR_DATA_DIR/agent-credentials. No HOST_* path is needed for these accounts.
#
# The provider-specific paths below are only for reusing credentials that
# already exist on the host. They must be absolute — do NOT use ~ or ${HOME}
# (neither is expanded in .env files parsed by Node or in Docker bind mounts).
# CLAUDE_CONFIG_PATH=/home/your-user/.claude
CLAUDE_CONFIG_PATH=
CLAUDE_MAX_TURNS=10
CLAUDE_TIMEOUT_MS=86400000
CODEX_TIMEOUT_MS=86400000
# Codex response-stream policy. WebSockets avoid infrastructure HTTP response
# deadlines during long, quiet model turns. Use "sse" when WebSockets are not
# available, or "inherit" to use the mounted Codex provider configuration.
CODEX_STREAM_TRANSPORT=websocket
CODEX_STREAM_IDLE_TIMEOUT_MS=1800000
CODEX_STREAM_MAX_RETRIES=5
CONTEXT_ANALYSIS_TIMEOUT_MS=3600000

# Antigravity Configuration
ANTIGRAVITY_TIMEOUT_MS=86400000

# OpenCode Configuration
OPENCODE_TIMEOUT_MS=86400000
# Existing-account credential mount paths for docker/launcher.
# HOST_OPENCODE_XDG_DIR points to the XDG config directory
# (/home/your-user/.config/opencode). The saved agent configPath still controls
# the runtime mount; the launcher sets OPENCODE_CONFIG_PATH only as the default
# path for worker/API processes.
# HOST_OPENCODE_DATA_DIR points to normal opencode auth data
# (/home/your-user/.local/share/opencode). Set it for launcher deployments
# so normal `opencode auth login` credentials are visible to spawned OpenCode
# agent containers and can refresh auth metadata when required.
# HOST_OPENCODE_XDG_DIR=/home/your-user/.config/opencode
# HOST_OPENCODE_DATA_DIR=/home/your-user/.local/share/opencode

# --- Mistral Vibe Configuration (only required when using a Vibe agent) ---
# Must be an absolute path — do NOT use ~ or ${HOME} (see CLAUDE_CONFIG_PATH note).
# VIBE_CONFIG_PATH=/home/your-user/.vibe
# Used when VIBE_CONFIG_PATH does not provide credentials.
# MISTRAL_API_KEY=
VIBE_MAX_TURNS=1000
VIBE_TIMEOUT_MS=86400000
# Production launcher only — host paths for reusing existing agent credential
# directories. ProPR-managed accounts do not require these settings. Omit any
# HOST_*_DIR to skip the corresponding existing-account mount.
# All HOST_*_DIR values MUST be absolute paths (no ~ or relative paths).
# HOST_CLAUDE_DIR=/home/your-user/.claude
# HOST_CODEX_DIR=/home/your-user/.codex
# Antigravity is Gemini-based and keeps its credentials under ~/.gemini, so the
# directory name intentionally differs from the variable name.
# HOST_ANTIGRAVITY_DIR=/home/your-user/.gemini
# HOST_VIBE_DIR=/home/your-user/.vibe
# Vibe Docker-outside-Docker writes prompt files to a host-visible directory so
# spawned agent containers can bind-mount them. When Vibe is enabled, the
# launcher defaults the host path to /tmp/propr-vibe-prompts-<uid>; the container
# path defaults to /tmp/propr-vibe-prompts. Set both only to override those
# locations.
# VIBE_PROMPT_CACHE_DIR=/tmp/propr-vibe-prompts
# HOST_VIBE_PROMPT_CACHE_DIR=/tmp/propr-vibe-prompts-1000

# Dashboard API Configuration
DASHBOARD_API_PORT=4000
# Browser origin: required in normal and demo mode for CORS and auth redirects.
# Defaults to http://localhost:5173 when unset. In hosted UI tunnel mode this is
# derived automatically as the hosted UI origin (https://app.propr.dev) — NOT the
# proxy host — so leave it COMMENTED to let derivation win. An uncommented value
# here always overrides the derived origin (set one only to force a custom
# origin). See the "Hosted UI tunnel" section above.
# FRONTEND_URL=http://localhost:5173
# Public URL the API is reached at (auth redirects, attachment links, cookie
# security). Defaults to http://localhost:4000 when unset; set it to the
# https://t-<id>.propr.dev host when the hosted UI tunnel is enabled.
# API_PUBLIC_URL=http://localhost:4000
# Optional lifetime for newly paired desktop instance tokens. When unset,
# tokens remain valid until the owner revokes them. Range: 1-3650 days.
# PROPR_DESKTOP_TOKEN_TTL_DAYS=90
# Optional per-IP desktop discovery/pairing quotas. Defaults are documented in
# docs/docs/operations/desktop-pairing.md.
# PROPR_DISCOVERY_RATE_LIMIT_MAX=60
# PROPR_PAIRING_START_RATE_LIMIT_MAX=10
# PROPR_PAIRING_POLL_RATE_LIMIT_MAX=180
# Session cookie domain. Leave UNSET for v1 — including hosted UI tunnel proxy
# sessions, which run on a single t-<id>.propr.dev host (see the tunnel
# section above). Only set it for a custom multi-subdomain deployment.
# COOKIE_DOMAIN=
# Optional comma-separated redirect hosts for auth preview flows.
# Entries are exact host matches by default. Prefix with "." or "*." only for
# parent domains where every subdomain is trusted, for example .preview.example.com.
# FRONTEND_URL is exact-only; COOKIE_DOMAIN allows subdomains when it starts with ".".
# AUTH_REDIRECT_ALLOWED_HOSTS=preview.example.com

# Web Push Configuration
# No keys are required: API startup generates and persists one instance identity
# in web-push/vapid.json beside the SQLite database (DB_FILENAME, else DATA_DIR,
# else ./data). Preserve this private directory in data-volume backups/restores.
# Subject defaults to the configured HTTPS API_PUBLIC_URL/FRONTEND_URL origin,
# falling back to https://propr.dev. A subject-only override is supported.
# WEB_PUSH_VAPID_SUBJECT=https://your-instance.example.com
# Optional manual override: provide BOTH keys from one P-256 pair. Invalid or
# partial pairs fail closed and are rejected by `propr check` and the launcher.
# Explicit keys take precedence without replacing the stored automatic pair.
# Keep private keys server-side; never commit, log, or put them in frontend config.
# WEB_PUSH_VAPID_PUBLIC_KEY=
# WEB_PUSH_VAPID_PRIVATE_KEY=
# Set false to skip automatic setup, enrollment and delivery. Preferences remain.
# WEB_PUSH_ENABLED=true
# Browser permission and each notification category still require user opt-in.
# Dispatcher polling interval and maximum jobs claimed per pass.
# WEB_PUSH_DISPATCH_INTERVAL_MS=5000
# WEB_PUSH_DISPATCH_BATCH_SIZE=20
# A claim must outlive the provider request timeout.
# WEB_PUSH_DELIVERY_LEASE_MS=60000
# WEB_PUSH_REQUEST_TIMEOUT_MS=15000
# Provider message lifetime, in seconds.
# WEB_PUSH_TTL_SECONDS=300
# WEB_PUSH_MAX_ATTEMPTS=5
# WEB_PUSH_RETRY_BASE_MS=30000
# WEB_PUSH_RETRY_CAP_MS=900000
# DANGEROUS local-only escape hatch. This is disabled by default. It permits the
# authenticated API to enroll HTTP loopback subscriptions for browser testing
# only when NODE_ENV is non-production and API_PUBLIC_URL is also loopback (or
# unset, whose default is localhost).
# PROPR_ALLOW_INSECURE_LOCAL_WEB_PUSH=false

# Per-client request quotas. Defaults protect the general API (600/minute),
# OAuth initiation/callback endpoints (30/15 minutes), and direct webhooks (300/minute).
# Values must be positive integers; raise them only for measured trusted traffic.
# PROPR_API_RATE_LIMIT_MAX=600
# PROPR_API_RATE_LIMIT_WINDOW_MS=60000
# PROPR_AUTH_RATE_LIMIT_MAX=30
# PROPR_AUTH_RATE_LIMIT_WINDOW_MS=900000
# PROPR_WEBHOOK_RATE_LIMIT_MAX=300
# PROPR_WEBHOOK_RATE_LIMIT_WINDOW_MS=60000
# Forwarded client IPs and HTTPS are accepted only from these immediate socket
# peers. Leave unset for direct access. The launcher uses its reserved `self`
# mode for the managed cloudflared sidecar; for another reverse proxy use
# `loopback`, or the proxy's exact IP/CIDR, as described in the deployment guide.
# PROPR_TRUSTED_PROXY_PEERS=loopback

# GitHub OAuth Configuration
# The OAuth callback is served by the API and is derived automatically when left
# unset: <proxy host>/api/auth/github/callback in hosted UI tunnel mode, or
# http://localhost:4000/api/auth/github/callback for local development. Leave it
# commented so enabling the tunnel (token/instance id) cannot leave a stale
# localhost callback that breaks hosted OAuth — an active localhost value is used
# as-is even in tunnel mode (the launcher only warns). Uncomment to override; then
# register that exact URL in your GitHub OAuth App.
# Relay-enrolled installs use PROPR_WEB_AUTH_MODE=connect automatically for both
# exact localhost callbacks and hosted tunnels. These OAuth client settings are
# only for an advanced custom/self-hosted web login.
# PROPR_WEB_AUTH_MODE=github
# GH_OAUTH_CLIENT_ID=your_github_oauth_client_id
# GH_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret
# GH_OAUTH_CALLBACK_URL=http://localhost:4000/api/auth/github/callback
SESSION_SECRET=your-session-secret-here

# Bearer Token Authentication (CLI)
# Set to 'false' to disable Bearer token auth and only allow session-based login
ENABLE_BEARER_AUTH=true

# Demo Mode
# Set to 'true' or '1' to allow read-only access without GitHub OAuth and block all mutating API requests.
# Public demo deployments should use a curated config/database.
PROPR_DEMO_MODE=false

# --- SQLite Database ---
# Path to SQLite database file (will be created if it doesn't exist)
DB_FILENAME=./data/propr.sqlite

# --- PR Preview Environment ---
# docker-compose.yml and scripts/deploy-pr.sh consume these local names.
# The PR Preview workflow maps repository variable PR_PREVIEW_STAGING_ENV_FILE
# to STAGING_ENV_FILE for deploys.
# Path to staging .env file (provides base configuration for PR previews)
# PR-specific values (ports, API URLs) are passed as inline overrides
STAGING_ENV_FILE=/path/to/staging/.env

# Optional path to a staging database file for seeding PR preview environments.
# The workflow maps repository variable PR_PREVIEW_STAGING_DB_PATH to this env.
STAGING_DB_PATH=/path/to/staging/data/propr.sqlite

# PR preview deploys are owned by the GitHub Actions workflow and run only after
# a maintainer adds the preview-env label to a pull request.
# Authenticated MCP (optional; disabled by default). See docs/mcp.md.
# MCP_PUBLIC_ORIGIN is the HTTPS API origin, without a path.
# Keep MCP_INSTANCE_ID stable. Generate a separate key with: openssl rand -base64 32
MCP_ENABLED=false
MCP_PUBLIC_ORIGIN=
MCP_INSTANCE_ID=
MCP_ENCRYPTION_KEY=
# Explicit, optional Connect delegation trust. These examples do not enable a gateway.
MCP_CONNECT_TRUST=false
MCP_CONNECT_ISSUER=
# Defaults to https://mcp.propr.dev; JWKS/resource/validation endpoints are derived.
# Connect reuses GH_INSTALLATION_ID and PROPR_GH_RELAY_TOKEN from relay setup,
# plus PROPR_UI_TUNNEL_ENABLED=true and PROPR_UI_TUNNEL_TOKEN.
# PROPR_INSTANCE_ID is the registered tunnel UUID; this optional override selects it.
MCP_CONNECT_TUNNEL_ID=
# After secure relay/tunnel setup, run npm run mcp:connect:register.
