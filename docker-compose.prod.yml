# Legacy source-build production compose. The supported deployment path is the
# launcher / prebuilt images (see docs: Production Deployment); this file builds
# from source and does not start the analysis-worker or indexing-worker
# services that the standard stack includes.
version: '3.8'

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - propr-redis-data:/data
    networks:
      - propr-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  api:
    build:
      context: .
      dockerfile: packages/api/Dockerfile.prod
      args:
        VITE_API_BASE_URL: ${FRONTEND_API_URL:-http://localhost}
        VITE_APP_URL: ${FRONTEND_APP_URL:-http://localhost}
    restart: unless-stopped
    ports:
      - "80:80"
      - "4000:4000"
    environment:
      NODE_ENV: production
      REDIS_HOST: redis
      REDIS_PORT: 6379
      DASHBOARD_API_PORT: 4000
      SESSION_SECRET: ${SESSION_SECRET}
      GH_OAUTH_CLIENT_ID: ${GH_OAUTH_CLIENT_ID}
      GH_OAUTH_CLIENT_SECRET: ${GH_OAUTH_CLIENT_SECRET}
      GH_OAUTH_CALLBACK_URL: ${GH_OAUTH_CALLBACK_URL}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      API_PUBLIC_URL: ${API_PUBLIC_URL:-http://localhost:4000}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      DB_FILENAME: /app/data/propr.sqlite
      GH_WEBHOOK_SECRET: ${GH_WEBHOOK_SECRET}
      SYSTEM_TASK_SECRET: ${SYSTEM_TASK_SECRET}
      WEB_PUSH_VAPID_SUBJECT: ${WEB_PUSH_VAPID_SUBJECT:-}
      WEB_PUSH_VAPID_PUBLIC_KEY: ${WEB_PUSH_VAPID_PUBLIC_KEY:-}
      WEB_PUSH_VAPID_PRIVATE_KEY: ${WEB_PUSH_VAPID_PRIVATE_KEY:-}
      WEB_PUSH_ENABLED: ${WEB_PUSH_ENABLED:-true}
      WEB_PUSH_DISPATCH_INTERVAL_MS: ${WEB_PUSH_DISPATCH_INTERVAL_MS:-5000}
      WEB_PUSH_DISPATCH_BATCH_SIZE: ${WEB_PUSH_DISPATCH_BATCH_SIZE:-20}
      WEB_PUSH_DELIVERY_LEASE_MS: ${WEB_PUSH_DELIVERY_LEASE_MS:-60000}
      WEB_PUSH_REQUEST_TIMEOUT_MS: ${WEB_PUSH_REQUEST_TIMEOUT_MS:-15000}
      WEB_PUSH_TTL_SECONDS: ${WEB_PUSH_TTL_SECONDS:-300}
      WEB_PUSH_MAX_ATTEMPTS: ${WEB_PUSH_MAX_ATTEMPTS:-5}
      WEB_PUSH_RETRY_BASE_MS: ${WEB_PUSH_RETRY_BASE_MS:-30000}
      WEB_PUSH_RETRY_CAP_MS: ${WEB_PUSH_RETRY_CAP_MS:-900000}
      PROPR_MANAGED_CREDENTIALS_DIR: ${HOME:?HOME must be set}/.propr/agent-credentials
      PROPR_CONTAINERIZED: "1"
      PROPR_STACK: ${PROPR_STACK:-propr}
      PROPR_API_RATE_LIMIT_MAX: ${PROPR_API_RATE_LIMIT_MAX:-600}
      PROPR_API_RATE_LIMIT_WINDOW_MS: ${PROPR_API_RATE_LIMIT_WINDOW_MS:-60000}
      PROPR_AUTH_RATE_LIMIT_MAX: ${PROPR_AUTH_RATE_LIMIT_MAX:-30}
      PROPR_AUTH_RATE_LIMIT_WINDOW_MS: ${PROPR_AUTH_RATE_LIMIT_WINDOW_MS:-900000}
      PROPR_WEBHOOK_RATE_LIMIT_MAX: ${PROPR_WEBHOOK_RATE_LIMIT_MAX:-300}
      PROPR_WEBHOOK_RATE_LIMIT_WINDOW_MS: ${PROPR_WEBHOOK_RATE_LIMIT_WINDOW_MS:-60000}
      PROPR_TRUSTED_PROXY_PEERS: ${PROPR_TRUSTED_PROXY_PEERS:-}
    volumes:
      - propr-sqlite-data:/app/data
      # The API starts short-lived sibling containers for agent image builds,
      # model discovery, and interactive Coding Agents logins. Docker-socket
      # access is root-equivalent host access; do not expose this API to
      # untrusted users and restrict access to the socket on the host.
      - /var/run/docker.sock:/var/run/docker.sock
      # ProPR-managed per-agent credentials. HOST:HOST keeps the path valid for
      # sibling containers started through the host Docker daemon.
      - ${HOME:?HOME must be set}/.propr/agent-credentials:${HOME:?HOME must be set}/.propr/agent-credentials
      # Add agent credential mounts with a deployment-specific override file.
      # Keeping them out of the base production compose file avoids Docker
      # creating missing host credential directories as root-owned folders.
      # Example OpenCode override (API only reads config/auth metadata):
      #   - /home/propr/.config/opencode:/home/propr/.config/opencode:ro
      #   - /home/propr/.local/share/opencode:/home/propr/.local/share/opencode:ro
    depends_on:
      redis:
        condition: service_healthy
      daemon:
        condition: service_started
    networks:
      - propr-network

  daemon:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      REDIS_HOST: redis
      REDIS_PORT: 6379
      GH_APP_ID: ${GH_APP_ID}
      GH_APP_PRIVATE_KEY: ${GH_APP_PRIVATE_KEY}
      GH_WEBHOOK_SECRET: ${GH_WEBHOOK_SECRET}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY:-}
      ALLOWED_ORGS: ${ALLOWED_ORGS}
      DB_FILENAME: /usr/src/app/data/propr.sqlite
    volumes:
      - ./logs:/usr/src/app/logs
      - propr-sqlite-data:/usr/src/app/data
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - propr-network
    command: ["node", "dist/src/daemon.js"]

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      REDIS_HOST: redis
      REDIS_PORT: 6379
      GH_APP_ID: ${GH_APP_ID}
      GH_APP_PRIVATE_KEY: ${GH_APP_PRIVATE_KEY}
      GH_WEBHOOK_SECRET: ${GH_WEBHOOK_SECRET}
      # Optional backend-only Actions dispatch credential; never set on daemon/API.
      PROPR_DEPLOYMENT_GITHUB_TOKEN: ${PROPR_DEPLOYMENT_GITHUB_TOKEN:-}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY:-}
      AGENT_CONTAINER_MEMORY_LIMIT: ${AGENT_CONTAINER_MEMORY_LIMIT:-6g}
      AGENT_CONTAINER_CPU_LIMIT: ${AGENT_CONTAINER_CPU_LIMIT:-}
      AGENT_CONTAINER_PIDS_LIMIT: ${AGENT_CONTAINER_PIDS_LIMIT:-512}
      # Vibe prompt cache — active with sane defaults so setting MISTRAL_API_KEY
      # is sufficient for API-key auth. PREFLIGHT: create the host directory
      # before starting (mkdir -p /tmp/propr-vibe-prompts && chown to your
      # worker user) — Docker creates missing bind-mount dirs as root.
      VIBE_PROMPT_CACHE_DIR: ${VIBE_PROMPT_CACHE_DIR:-/tmp/propr-vibe-prompts}
      HOST_VIBE_PROMPT_CACHE_DIR: ${HOST_VIBE_PROMPT_CACHE_DIR:-/tmp/propr-vibe-prompts}
      # --- Vibe agent (config-file auth, alternative to API-key) ---
      VIBE_CONFIG_PATH: ${VIBE_CONFIG_PATH:-/root/.vibe}
      ALLOWED_ORGS: ${ALLOWED_ORGS}
      DB_FILENAME: /usr/src/app/data/propr.sqlite
      SYSTEM_TASK_SECRET: ${SYSTEM_TASK_SECRET}
      PROPR_MANAGED_CREDENTIALS_DIR: ${HOME:?HOME must be set}/.propr/agent-credentials
      PROPR_CONTAINERIZED: "1"
    volumes:
      - ./logs:/usr/src/app/logs
      - ./repos:/usr/src/app/repos
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME:?HOME must be set}/.propr/agent-credentials:${HOME:?HOME must be set}/.propr/agent-credentials
      - propr-sqlite-data:/usr/src/app/data
      # Add Claude/Codex/Gemini/OpenCode credential mounts with a deployment-
      # specific override file. The launcher conditionally mounts HOST_*
      # credential directories and is the preferred production path for agent
      # credential auth. Vibe keeps the legacy defaults below for compatibility.
      # Example OpenCode override:
      #   environment:
      #     OPENCODE_CONFIG_PATH: /home/propr/.config/opencode
      #     HOST_OPENCODE_DATA_DIR: /home/propr/.local/share/opencode
      #   volumes:
      #     - /home/propr/.config/opencode:/home/propr/.config/opencode:ro
      #     - /home/propr/.local/share/opencode:/home/propr/.local/share/opencode:rw
      # Vibe prompt cache — mounted by default so API-key auth works without
      # manual edits. PREFLIGHT: create and own the host directory before starting:
      #   mkdir -p "${HOST_VIBE_PROMPT_CACHE_DIR:-/tmp/propr-vibe-prompts}"
      - ${HOST_VIBE_PROMPT_CACHE_DIR:-/tmp/propr-vibe-prompts}:/tmp/propr-vibe-prompts
      # Vibe config-file auth. Mounts host config to the same path the worker
      # passes to sibling agent containers.
      - ${HOST_VIBE_DIR:-/root/.vibe}:${VIBE_CONFIG_PATH:-/root/.vibe}:ro
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - propr-network
    command: ["node", "dist/src/worker.js"]

networks:
  propr-network:
    driver: bridge

volumes:
  propr-redis-data:
  propr-sqlite-data:
